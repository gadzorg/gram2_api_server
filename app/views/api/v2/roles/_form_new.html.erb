<%= form_for @role, as: :role, url: url_for(controller: "roles", action: "create") do |f| %>

    <%= render 'form_fields', f: f %>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
