<%= form_for @group, as: :group, url: url_for(controller: "groups", action: "update") do |f| %>

    <%= render 'form_fields', f: f %>

    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
